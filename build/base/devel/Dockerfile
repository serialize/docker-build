FROM serialize/arch-minimal
MAINTAINER Frank Binder docker@serialize.org

RUN pacman -Sy tar gcc make autoconf automake binutils fakeroot file gzip libtool m4 patch pkg-config --noconfirm && \
    pacman -Scc --noconfirm

 
